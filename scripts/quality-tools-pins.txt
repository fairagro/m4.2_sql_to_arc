# Fleet quality CLI pins for hooks/CI (`scripts/run-quality-cli.sh`).
# Synced SoT — products do not need these in pyproject for gates to spawn.
# IDE MAY still list the same tools as optional project deps.
ruff>=0.15.0
mypy>=1.14.0
pylint>=3.3.0
bandit>=1.7.0
vulture>=2.14
import-linter>=2.1
ggshield>=1.43.0
