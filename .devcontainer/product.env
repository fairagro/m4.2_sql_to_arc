# Product-owned Dev Container env overlays (not synced).
# Loaded via docker-compose env_file; keep in sync with CI / quality defaults.
MYPYPATH=middleware/sql_to_arc/src
CST_BAKE_TARGET=sql_to_arc
CST_IMAGE_TAG=sql-to-arc:test
CST_CONFIG=docker/container-structure-tests
