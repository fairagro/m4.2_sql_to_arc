# Product-owned Dev Container env (not synced). Loaded via docker-compose
# env_file (optional). See docs/devcontainer.md and issue #129.
#
# Path overlays for synced pre-commit / mypy / CST — do not put these in
# synced .devcontainer/devcontainer.json.

MYPYPATH=stubs:middleware/sql_to_arc/src
CST_BAKE_TARGET=sql_to_arc
CST_IMAGE_TAG=sql-to-arc:test
CST_CONFIG=docker/container-structure-tests
