{
  "name": "m4.2_sql_to_arc",
  "private": true,
  "description": "FAIRagro SQL-to-ARC converter — markdown toolchain pins mirror versions.env",
  "scripts": {
    "format:md": "prettier --write \"**/*.{md,mdc}\"",
    "format:md:check": "prettier --check \"**/*.{md,mdc}\"",
    "lint:md": "markdownlint-cli2"
  },
  "devDependencies": {
    "markdownlint-cli2": "0.23.2",
    "prettier": "3.9.6"
  },
  "engines": {
    "node": ">=22"
  }
}
